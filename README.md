# Agent Dashboard

## Backend Security Setup

1. Copy `backend/.env.example` to `backend/.env`.
2. Fill in:
   - `PANDA_GATEWAY_TOKEN`
   - `BANANA_GATEWAY_TOKEN`
   - `APPLE_GATEWAY_TOKEN`
3. Optional: set `ALLOWED_ORIGINS` for frontend domains.
