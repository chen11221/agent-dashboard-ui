PANDA_GATEWAY_TOKEN=replace_with_panda_token
BANANA_GATEWAY_TOKEN=replace_with_banana_token
APPLE_GATEWAY_TOKEN=replace_with_apple_token
# Optional. If empty, all origins are allowed.
# To enforce CORS, set comma-separated origins, e.g.
# ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ALLOWED_ORIGINS=
